const TrustBar = () => (
  <section className="border-y border-line bg-[#f9fafc]">
    <div className="mx-auto flex w-full max-w-6xl flex-wrap items-center justify-center gap-3 px-6 py-8 text-center text-sm font-semibold uppercase tracking-wide text-muted">
      <span className="opacity-80">
        Trusted by thousands of Main Street business owners nationwide
      </span>
    </div>
  </section>
);

export default TrustBar;
